import { SortOption } from '../../_entities/sortOption';

export const DEFAULT_SORT_OPTION: SortOption = {
  field: 'date',
  order: 'desc',
  searchType: 'title',
  searchValue: '',
} as SortOption;
